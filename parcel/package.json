{
  "name": "parcel-test",
  "version": "1.0.0",
  "main": "dist/index.html",
  "license": "MIT",
  "devDependencies": {
    "parcel": "^2.0.0-beta.1",
    "sass": "^1.26.3"
  },
  "scripts": {
    "build": "parcel build src/index.html",
    "start": "parcel serve src/index.html",
    "start:dev": "parcel watch src/index.html",
    "clean": "rm -rf dist .parcel-cache .cache"
  }
}
